---
import Layout from '@/layouts/Layout.astro';
import { MapPin, ExternalLink, Store, ShoppingCart, Search } from '@lucide/astro';

const retailers = [
  {
    name: 'dm-drogerie markt',
    logo: '/retailers/dm.svg',
    url: 'https://www.dm.de/search?query=elmex',
    type: 'store',
    description: 'Online & in über 2.000 Filialen',
    shortName: 'dm'
  },
  {
    name: 'Rossmann',
    logo: '/retailers/rossmann.svg',
    url: 'https://www.rossmann.de/search?text=elmex',
    type: 'store',
    description: 'Online & in über 2.200 Filialen',
    shortName: 'R'
  },
  {
    name: 'Amazon',
    logo: '/retailers/amazon.svg',
    url: 'https://www.amazon.de/s?k=elmex',
    type: 'online',
    description: 'Versand in 1-2 Tagen',
    shortName: '📦'
  },
  {
    name: 'DocMorris',
    logo: '/retailers/docmorris.svg',
    url: 'https://www.docmorris.de/search?searchTerm=elmex',
    type: 'online',
    description: 'Online-Apotheke',
    shortName: '+'
  },
  {
    name: 'Müller',
    logo: '/retailers/mueller.svg',
    url: 'https://www.mueller.de/search/?text=elmex',
    type: 'store',
    description: 'Online & in über 560 Filialen',
    shortName: 'M'
  },
  {
    name: 'REWE',
    logo: '/retailers/rewe.svg',
    url: 'https://shop.rewe.de/products/search?search=elmex',
    type: 'store',
    description: 'Online & in Supermärkten',
    shortName: 'R'
  }
];

// Product name mapping for display
const productNames: Record<string, string> = {
  'kariesschutz-zahnpasta': 'elmex KARIESSCHUTZ Zahnpasta',
  'sensitive-zahnpasta': 'elmex SENSITIVE Zahnpasta',
  'kariesschutz-mundspuelung': 'elmex KARIESSCHUTZ Mundspülung',
  'sensitive-mundspuelung': 'elmex SENSITIVE Mundspülung',
  'sensitive-professional': 'elmex SENSITIVE PROFESSIONAL',
  'white': 'elmex OPTI-SCHMELZ WHITE',
  'junior': 'elmex JUNIOR',
  'junior-mundspuelung': 'elmex JUNIOR Mundspülung',
  'zahnbuerste': 'elmex Zahnbürste',
  'gelee': 'elmex gelée'
};
---

<Layout
  title="Wo kaufen | elmex"
  description="Finden Sie elmex Produkte in Ihrer Nähe oder online. Verfügbar in Apotheken, Drogeriemärkten und Online-Shops."
  keywords="elmex kaufen, Händler, Apotheke, dm, Rossmann, Müller, Amazon"
>
  <div class="container mx-auto px-4 py-16">
    <!-- Hero Section -->
    <div class="max-w-4xl mx-auto text-center mb-16">
      <h1 id="page-title" class="text-4xl md:text-5xl font-brand font-bold text-neutral-900 mb-4">
        Wo Sie elmex kaufen können
      </h1>
      <p id="page-subtitle" class="text-lg text-neutral-600 mb-8">
        elmex Produkte sind deutschlandweit in Apotheken, Drogeriemärkten und online erhältlich
      </p>
    </div>

    <!-- Store Locator -->
    <div class="max-w-2xl mx-auto mb-16 bg-gradient-to-br from-primary/5 to-primary/10 rounded-2xl p-8 border border-primary/20">
      <h2 class="text-2xl md:text-3xl font-brand font-bold mb-4 flex items-center gap-3 text-neutral-900">
        <MapPin class="w-8 h-8 text-primary flex-shrink-0" />
        Filiale in Ihrer Nähe finden
      </h2>
      <p class="text-neutral-600 mb-6">
        Geben Sie Ihre Postleitzahl oder Ihren Ort ein, um die nächstgelegenen Geschäfte zu finden
      </p>
      <form id="store-locator-form" class="flex flex-col sm:flex-row gap-3">
        <div class="relative flex-1">
          <input
            type="text"
            id="location-input"
            placeholder="PLZ oder Ort eingeben (z.B. 10115 oder Berlin)"
            class="w-full px-4 py-3 pl-12 rounded-lg border-2 border-neutral-300 bg-white text-neutral-900 focus:border-primary focus:outline-none transition-colors"
            required
          />
          <Search class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-neutral-400" />
        </div>
        <button type="submit" class="inline-flex items-center justify-center px-8 py-3 text-base font-medium text-white bg-primary hover:bg-primary-dark transition-colors rounded-lg">
          Suchen
        </button>
      </form>
      <div id="location-results" class="hidden mt-6">
        <h3 class="font-semibold mb-3 text-neutral-900">Geschäfte in Ihrer Nähe:</h3>
        <div id="results-container" class="space-y-3">
          <!-- Dynamically populated -->
        </div>
      </div>
    </div>

    <!-- Category Filters -->
    <div class="max-w-4xl mx-auto mb-8">
      <div class="flex flex-wrap justify-center gap-3">
        <button class="filter-btn active" data-filter="all">
          Alle anzeigen
        </button>
        <button class="filter-btn" data-filter="online">
          <ShoppingCart class="w-4 h-4" />
          Online
        </button>
        <button class="filter-btn" data-filter="store">
          <Store class="w-4 h-4" />
          Vor Ort
        </button>
      </div>
    </div>

    <!-- Retailer Grid -->
    <div class="max-w-5xl mx-auto mb-16">
      <h2 class="text-2xl md:text-3xl font-brand font-bold text-center mb-8 text-neutral-900">
        Unsere Handelspartner
      </h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {retailers.map(retailer => (
          <div
            class="retailer-card bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all"
            data-type={retailer.type}
          >
            <div class="flex items-center gap-4 mb-4">
              <div class="w-16 h-16 bg-neutral-100 rounded-lg flex items-center justify-center">
                <span class={`text-2xl font-bold ${
                  retailer.name === 'dm-drogerie markt' ? 'text-primary' :
                  retailer.name === 'Rossmann' ? 'text-red-600' :
                  retailer.name === 'DocMorris' ? 'text-green-600' :
                  retailer.name === 'Müller' ? 'text-orange-600' :
                  retailer.name === 'REWE' ? 'text-red-700' :
                  'text-neutral-700'
                }`}>{retailer.shortName}</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg text-neutral-900">{retailer.name}</h3>
                <p class="text-sm text-neutral-600">{retailer.description}</p>
              </div>
            </div>
            <a
              href={retailer.url}
              target="_blank"
              rel="noopener noreferrer"
              class="block w-full px-6 py-3 bg-primary hover:bg-primary-dark text-white text-center font-semibold rounded-full transition-colors"
            >
              Bei {retailer.name === 'dm-drogerie markt' ? 'dm' : retailer.name.split(' ')[0]} kaufen
            </a>
          </div>
        ))}
      </div>
    </div>

    <!-- Additional Info -->
    <div class="max-w-4xl mx-auto grid md:grid-cols-2 gap-8">
      <!-- Online Shopping Benefits -->
      <div class="bg-white rounded-xl p-6 border border-neutral-200">
        <div class="flex items-start gap-4">
          <div class="flex-shrink-0 w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
            <ShoppingCart class="w-6 h-6 text-primary" />
          </div>
          <div>
            <h3 class="text-xl font-brand font-semibold mb-2 text-neutral-900">Online bestellen</h3>
            <p class="text-neutral-600 mb-3">
              Bequem von zu Hause aus einkaufen
            </p>
            <ul class="space-y-2 text-sm text-neutral-600">
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Schnelle Lieferung
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Große Produktauswahl
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Bequem nach Hause
              </li>
            </ul>
          </div>
        </div>
      </div>

      <!-- In-Store Shopping Benefits -->
      <div class="bg-white rounded-xl p-6 border border-neutral-200">
        <div class="flex items-start gap-4">
          <div class="flex-shrink-0 w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
            <Store class="w-6 h-6 text-primary" />
          </div>
          <div>
            <h3 class="text-xl font-brand font-semibold mb-2 text-neutral-900">Vor Ort kaufen</h3>
            <p class="text-neutral-600 mb-3">
              Persönliche Beratung in Ihrer Nähe
            </p>
            <ul class="space-y-2 text-sm text-neutral-600">
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Persönliche Beratung
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Sofort verfügbar
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Keine Versandkosten
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="max-w-3xl mx-auto mt-16 text-center bg-neutral-100 rounded-2xl p-8">
      <h3 class="text-2xl font-brand font-bold mb-3 text-neutral-900">
        Nicht sicher, welches Produkt Sie brauchen?
      </h3>
      <p class="text-neutral-600 mb-6">
        Nutzen Sie unseren Produktfinder für eine persönliche Empfehlung
      </p>
      <a
        href="/beratung/produktfinder"
        class="inline-flex items-center justify-center px-8 py-3 text-base font-medium text-white bg-primary hover:bg-primary-dark transition-colors rounded-full"
      >
        Zum Produktfinder
      </a>
    </div>
  </div>
</Layout>

<style>
  .filter-btn {
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 500;
    border: 2px solid #D4D4D4;
    color: #525252;
    transition: all 0.3s;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .filter-btn:hover {
    border-color: #FF6000;
    color: #FF6000;
  }

  .filter-btn.active {
    background-color: #FF6000;
    color: white;
    border-color: #FF6000;
  }

  .retailer-card {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .retailer-card:hover {
    transform: translateY(-4px);
  }
</style>

<script>
  // Category filtering
  document.addEventListener('DOMContentLoaded', () => {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const retailerCards = document.querySelectorAll('.retailer-card');

    filterBtns.forEach(btn => {
      btn.addEventListener('click', function(this: HTMLElement) {
        // Update active state
        filterBtns.forEach(b => b.classList.remove('active'));
        this.classList.add('active');

        // Filter retailers
        const filter = this.getAttribute('data-filter');

        retailerCards.forEach(card => {
          const cardType = card.getAttribute('data-type');
          const htmlCard = card as HTMLElement;

          if (filter === 'all' || cardType === filter) {
            htmlCard.style.display = 'block';
            setTimeout(() => {
              htmlCard.style.opacity = '1';
              htmlCard.style.transform = 'translateY(0)';
            }, 10);
          } else {
            htmlCard.style.opacity = '0';
            htmlCard.style.transform = 'translateY(10px)';
            setTimeout(() => {
              htmlCard.style.display = 'none';
            }, 300);
          }
        });
      });
    });

    // Store Locator Functionality
    const storeLocatorForm = document.getElementById('store-locator-form');
    const locationInput = document.getElementById('location-input') as HTMLInputElement;
    const locationResults = document.getElementById('location-results');
    const resultsContainer = document.getElementById('results-container');

    storeLocatorForm?.addEventListener('submit', async (e) => {
      e.preventDefault();

      const location = locationInput.value.trim();

      if (!location) {
        alert('Bitte geben Sie eine Postleitzahl oder einen Ort ein');
        return;
      }

      // Show loading state
      if (resultsContainer) {
        resultsContainer.innerHTML = '<p class="text-neutral-600">Suche nach Geschäften...</p>';
      }
      locationResults?.classList.remove('hidden');

      // Simulate API call (in real implementation, would call Google Places API or similar)
      setTimeout(() => {
        // Mock results
        const mockStores = [
          { name: 'dm-drogerie markt', address: 'Hauptstraße 123, ' + location, distance: '0.5 km', url: 'https://www.dm.de' },
          { name: 'Rossmann', address: 'Bahnhofstraße 45, ' + location, distance: '1.2 km', url: 'https://www.rossmann.de' },
          { name: 'Müller', address: 'Marktplatz 7, ' + location, distance: '1.8 km', url: 'https://www.mueller.de' }
        ];

        if (resultsContainer) {
          resultsContainer.innerHTML = mockStores.map(store => `
            <div class="bg-white rounded-lg p-4 border border-neutral-200 flex items-start justify-between gap-4">
              <div class="flex-1">
                <h4 class="font-semibold text-neutral-900">${store.name}</h4>
                <p class="text-sm text-neutral-600 mt-1">${store.address}</p>
                <p class="text-xs text-primary font-medium mt-2">📍 ${store.distance} entfernt</p>
              </div>
              <a
                href="${store.url}"
                target="_blank"
                rel="noopener noreferrer"
                class="flex-shrink-0 px-4 py-2 text-sm font-medium text-primary border-2 border-primary hover:bg-primary hover:text-white transition-colors rounded-lg"
              >
                Details
              </a>
            </div>
          `).join('');
        }

        // Track search
        if (typeof gtag !== 'undefined') {
          gtag('event', 'store_locator_search', {
            'search_term': location
          });
        }
      }, 1000);
    });

    // Get product from URL parameter and update title
    const urlParams = new URLSearchParams(window.location.search);
    const productSlug = urlParams.get('product');

    // Product name mapping
    const productNamesMap: Record<string, string> = {
      'kariesschutz-zahnpasta': 'elmex KARIESSCHUTZ Zahnpasta',
      'sensitive-zahnpasta': 'elmex SENSITIVE Zahnpasta',
      'kariesschutz-mundspuelung': 'elmex KARIESSCHUTZ Mundspülung',
      'sensitive-mundspuelung': 'elmex SENSITIVE Mundspülung',
      'sensitive-professional': 'elmex SENSITIVE PROFESSIONAL',
      'white': 'elmex OPTI-SCHMELZ WHITE',
      'junior': 'elmex JUNIOR',
      'junior-mundspuelung': 'elmex JUNIOR Mundspülung',
      'zahnbuerste': 'elmex Zahnbürste',
      'gelee': 'elmex gelée'
    };

    if (productSlug) {
      const productName = productNamesMap[productSlug] || 'elmex Produkt';

      // Update page title
      const pageTitle = document.getElementById('page-title');
      if (pageTitle) {
        pageTitle.innerHTML = `Wo kaufen: <span class="text-primary">${productName}</span>`;
      }

      // Update subtitle
      const pageSubtitle = document.getElementById('page-subtitle');
      if (pageSubtitle) {
        pageSubtitle.textContent = 'Hier finden Sie alle Händler, bei denen Sie dieses Produkt kaufen können';
      }
    }
  });
</script>
